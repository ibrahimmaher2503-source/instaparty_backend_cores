---
## REQUIRED CONTEXT (load before executing this command)

Before generating any artifact, you MUST silently read these files in order:
1. .specify/memory/constitution.md
2. .specify/memory/project-index.md
3. .specify/memory/api-registry.md
4. docs/specs/01_PRD.md
5. docs/specs/09_Phasing_Plan.md
6. docs/specs/11_DB_Schema.md
7. docs/specs/10_Package_List.md

CONSTRAINTS (non-negotiable):
- Every generated artifact must cite specific FR numbers from 01_PRD.md
- Every generated artifact must cite specific table names from 11_DB_Schema.md
- Every generated artifact must align to a Phase ID from 09_Phasing_Plan.md
- Never suggest a package not in 10_Package_List.md
- Never suggest a Phase 2 feature
- Never contradict docs/specs/02_Tech_Decisions.md locked stack

API DOCUMENTATION CONSTRAINT:
- Every API endpoint generated must include:
  - @bodyParam PHPDoc on every Form Request field (Scribe-compatible)
  - @response PHPDoc with realistic EN+AR example data on every Resource
  - Entry added to .specify/memory/api-registry.md
  - Bruno/Postman collection entry in docs/api/collections/
---

# InstaParty Feature Checklist: [FEATURE NAME]

**Purpose**: Validate that the feature spec, plan, and implementation align with InstaParty Backend Constitution principles I-XI and respect Phase 1 boundaries.

**Created**: [DATE]
**Feature**: [Link to spec.md]
**Phase ID**: [Phase X.Y from `docs/specs/09_Phasing_Plan_v2.md`]
**Module(s) Touched**: [list under `app/Modules/`]
**ADR(s)**: [ADR-NNNN-{slug}.md — required if new module]

**Note**: This checklist is generated by `/speckit.checklist` based on the feature spec, the InstaParty constitution at `.specify/memory/constitution.md`, and the relevant phase from `docs/specs/09_Phasing_Plan_v2.md`. Each item links to its source. Strike through items that do not apply to this feature with a brief justification.

<!--
  ============================================================================
  CHECKLIST GENERATION RULES (for /speckit.checklist):

  1. Categories below are MANDATORY — preserve all category headers
  2. Within each category, replace sample items with feature-specific items
  3. Phase 1 forbidden features (PRD §5.2) MUST be flagged in Category K
  4. Type-aware features MUST have explicit rental/sale/digital items
  5. Bilingual items MUST appear for any user-facing text
  6. Money-touching features MUST have full Money Discipline checklist
  7. New modules MUST require an ADR — flag if missing
  8. Items MUST cite the constitution principle (I-XI) or spec section
  ============================================================================
-->

## A. Spec-Kit Artifact Discipline

- [ ] CHK001 Spec declares **Phase ID** matching `docs/specs/09_Phasing_Plan_v2.md` (Constitution §"Phasing Discipline")
- [ ] CHK002 Spec lists **PRD requirements covered** (FR-NN numbers from `docs/specs/01_PRD.md`)
- [ ] CHK003 Spec lists **tables touched** (only ones THIS phase creates/modifies, cited from `docs/specs/11_DB_Schema.md`)
- [ ] CHK004 Spec includes **Constitution Check** section with pass/fail for each applicable principle (I-XI)
- [ ] CHK005 Spec includes a **Cut-list** inherited from the phase definition
- [ ] CHK006 Spec includes 3-5 **Exit Criteria** as concrete checkboxes
- [ ] CHK007 Plan cites **specific spec sections** (e.g., "Tech Decisions §11", "Schema §3", "PRD FR-19") — not paraphrased
- [ ] CHK008 Plan declares **architecture tests** added or updated for this feature

## B. Module Boundaries (Constitution §I)

- [ ] CHK010 New code lives under `app/Modules/{ModuleName}/` with the standard layer structure (Domain / Application / Infrastructure / Filament / Routes / Database)
- [ ] CHK011 Cross-module communication uses **domain events** or **public Contracts** — never direct Eloquent Model imports
- [ ] CHK012 Public Contracts (interfaces) live in `Modules/{Name}/Domain/Contracts/`
- [ ] CHK013 If a new module is introduced, an **ADR exists with status `Accepted`** in `docs/adr/NNNN-{slug}.md` BEFORE any migration is written
- [ ] CHK014 Architecture test `tests/Architecture/NoCrossModuleModelImportsTest.php` still passes after this feature
- [ ] CHK015 Models hold **only** relationships, casts, scopes — NO business logic in Models

## C. Three Product Types (Constitution §II — NON-NEGOTIABLE)

- [ ] CHK020 If type-aware: feature has **separate** `Create{Rental|Sale|Digital}{X}Action` classes (or equivalent layer)
- [ ] CHK021 Cross-type code uses `match($productType)` on `App\Modules\Catalog\Domain\Enums\ProductType` — NEVER `if/elseif` on type strings
- [ ] CHK022 Per-type Form Requests exist for any type-aware input (Rental + Sale + Digital)
- [ ] CHK023 Per-type API Resources exist for any type-aware output
- [ ] CHK024 Per-type Filament Resources exist if Filament UI is in scope
- [ ] CHK025 Pest tests have explicit cases: `it('handles rental ...')`, `it('handles sale ...')`, `it('handles digital ...')`
- [ ] CHK026 Polymorphic detail tables used (`service_rental_details` / `service_sale_details` / `service_digital_details`) — STI is forbidden
- [ ] CHK027 Architecture test `tests/Architecture/NoIfElseOnProductTypeStringTest.php` still passes

## D. Money Discipline (Constitution §III — NON-NEGOTIABLE)

- [ ] CHK030 Every money column is `BIGINT UNSIGNED` named `{field}_minor` + `CHAR(3)` `{field}_currency`
- [ ] CHK031 Casts route through `App\Modules\Shared\Domain\Casts\MoneyCast` — never raw int access for currency-bearing fields
- [ ] CHK032 No `float` / `decimal` types appear anywhere in the feature's PHP code (parameters, return types, casts)
- [ ] CHK033 All arithmetic uses `Brick\Money\Money` methods with explicit rounding mode where required
- [ ] CHK034 Display formatting routed through locale-aware money formatter — never inline `number_format`
- [ ] CHK035 Pest test `MoneyCastTest` covers any new money column added by this feature
- [ ] CHK036 Architecture test `tests/Architecture/NoFloatForMoneyTest.php` still passes

## E. Bilingual EN+AR (Constitution §IV — NON-NEGOTIABLE)

- [ ] CHK040 Translatable text fields use **JSON columns** + `spatie/laravel-translatable`
- [ ] CHK041 Both `en` and `ar` keys are required by Form Request validation — empty strings rejected
- [ ] CHK042 API Resources convert to `App::getLocale()` for response (single-locale output)
- [ ] CHK043 Filament Resources use translatable plugin with EN/AR tabs
- [ ] CHK044 Validation error messages exist in both `resources/lang/en/{file}.php` and `resources/lang/ar/{file}.php`
- [ ] CHK045 Pest tests assert response shape in **both** EN and AR for each user-facing endpoint
- [ ] CHK046 Filament panel renders correctly in AR (RTL) — visual QA done if Filament is in scope

## F. Append-Only Tables (Constitution §V — NON-NEGOTIABLE)

- [ ] CHK050 No `softDeletes()` added to any append-only table (wallet_ledger, audit_logs, payments, commissions, booking_state_transitions, event_outbox, analytics_events, loyalty_ledger, booking_snapshots, chat_message_log, search_logs)
- [ ] CHK051 No code path UPDATES an append-only row except for documented mutable columns (per Constitution §V table)
- [ ] CHK052 Reversals (e.g., refund reversing wallet credit) use **counter-entries**, not edits
- [ ] CHK053 Architecture test `tests/Architecture/AppendOnlyTablesHaveNoSoftDeletesTest.php` still passes

## G. Idempotency (Constitution §VIII)

- [ ] CHK060 State-changing endpoint (POST/PATCH/DELETE) accepts `Idempotency-Key` header where required by Constitution §VIII
- [ ] CHK061 `idempotency_keys` table records `(key, user_id, request_hash, response_hash, status_code, expires_at)`
- [ ] CHK062 Same key + same body returns cached response
- [ ] CHK063 Same key + different body returns `409 Conflict`
- [ ] CHK064 Pest test asserts idempotency under concurrent requests (race condition coverage)

## H. Domain Events & Transactions (Constitution §IX)

- [ ] CHK070 Domain events fire only via `DB::afterCommit(fn () => Event::dispatch(...))` — never inside transaction sync
- [ ] CHK071 Listeners that send notifications, dispatch jobs, or call external APIs queue their work — no sync external I/O inside transactions
- [ ] CHK072 Event class lives in `Modules/{Module}/Domain/Events/` — module-internal
- [ ] CHK073 Listeners live in `Modules/{Module}/Application/Listeners/`
- [ ] CHK074 Events documented in the ADR if cross-module consumption is expected

## I. Migrations & Schema (Constitution §I + Schema discipline)

- [ ] CHK080 Migration uses `utf8mb4` charset + `utf8mb4_unicode_ci` collation in every `Schema::create`
- [ ] CHK081 User-facing entity has `public_id CHAR(26)` ULID + index — internal `id BIGINT` for FKs
- [ ] CHK082 Foreign keys default to `restrictOnDelete()` — `cascadeOnDelete()` only when domain-correct (e.g., service detail tables to base service)
- [ ] CHK083 Indexes added for: foreign keys, status enums, frequently-queried columns
- [ ] CHK084 Migration matches the schema in `docs/specs/11_DB_Schema.md` — deviations require schema doc update first
- [ ] CHK085 Down migration is correct (drops in reverse FK order)
- [ ] CHK086 Migration tested via `migrate:fresh --seed` end-to-end

## J. Tests (Constitution §VII — NON-NEGOTIABLE for critical paths)

- [ ] CHK090 Pest tests cover happy path
- [ ] CHK091 Pest tests cover auth (401 when unauthenticated)
- [ ] CHK092 Pest tests cover authorization (403 when unauthorized)
- [ ] CHK093 Pest tests cover validation (422 for invalid input)
- [ ] CHK094 Pest tests cover idempotency where applicable
- [ ] CHK095 Pest tests cover both EN and AR locales
- [ ] CHK096 Type-aware tests cover **all three** product types (rental + sale + digital)
- [ ] CHK097 Coverage threshold: 80%+ on Action classes for auth/payments/bookings; 60%+ otherwise
- [ ] CHK098 Tests written **the same day** as the code (per Constitution §VII test-with-code rule)
- [ ] CHK099 Pest groups used (`->group('module-name')`, `->group('rental')`, etc.) for selective test runs

## K. Phase 1 Scope Guard (Constitution §"Phase 1 Forbidden Features")

For each item below, confirm it is NOT introduced by this feature:

- [ ] CHK110 No vendor subscription tiers (silver/gold/bronze) — Phase 2
- [ ] CHK111 No platform-owned package products — Phase 2
- [ ] CHK112 No dispute-resolution engine with automated penalties — Phase 2
- [ ] CHK113 No per-category card-template UI — Phase 2
- [ ] CHK114 No vendor page slider — Phase 2
- [ ] CHK115 No vendor QR/barcode catalog — Phase 2
- [ ] CHK116 No advanced tax invoicing beyond foundational structure — Phase 2
- [ ] CHK117 No multi-currency activation (schema ready, activation Phase 2)
- [ ] CHK118 No GCC payment gateways (Tabby, Tamara, HyperPay) — Phase 2

If any item must be checked false for legitimate reasons, an ADR amending the constitution must precede the feature.

## L. Locked Stack Compliance (Constitution §"Locked Tech Stack")

- [ ] CHK120 No new package added without entry in `docs/specs/10_Package_List.md`
- [ ] CHK121 No package replaces a locked alternative (e.g., no Provider when GetX is locked, no Algolia when Meilisearch is locked, no Stripe when Paymob is locked for Phase 1)
- [ ] CHK122 Version pinned in `composer.json` (no `^` for breaking-change-prone packages)
- [ ] CHK123 If new package introduced, rationale + alternatives considered are documented

## M. Filament Admin Discipline (when Filament is in scope)

- [ ] CHK130 Filament Resource auto-discovered from `app/Modules/{Name}/Filament/Resources/`
- [ ] CHK131 Translatable fields use `spatie/laravel-translatable` plugin tabs (EN/AR)
- [ ] CHK132 Navigation grouping is intentional (`Services`, `Bookings`, `Vendors`, etc.) — not flat
- [ ] CHK133 Type-aware Filament Resources are separate (`RentalServiceResource` ≠ `SaleServiceResource`)
- [ ] CHK134 Permissions checked via Spatie Shield (`shield:generate --all` rerun if new resource)
- [ ] CHK135 Bulk actions defined where relevant (mass approve, bulk export)
- [ ] CHK136 RTL render verified for AR locale (visual QA in Filament Day)

## N. Vendor Approval Discipline (Constitution §X — when feature touches vendors)

- [ ] CHK140 Profile-level approval is gated BEFORE per-type approval (`approval_status='approved'` check in `ApproveVendorForTypeAction`)
- [ ] CHK141 Suspension/rejection of profile auto-revokes active type approvals via listener
- [ ] CHK142 Both decision tables logged: `vendor_profiles.approval_status` history + `vendor_approved_product_types`
- [ ] CHK143 Audit log captures every approval/revocation with reviewer, timestamp, reason

## O. Document Storage Discipline (Constitution §XI — when feature stores files)

- [ ] CHK150 Typed documents (vendor docs, settlement proofs) use **Direct S3 private** with `file_path`/`file_name` columns
- [ ] CHK151 Galleries/avatars use **spatie/laravel-medialibrary** on S3 public
- [ ] CHK152 Chat media routes to **Firebase Storage** (separate auth model)
- [ ] CHK153 Signed URL access used for private documents (no public URL exposure)
- [ ] CHK154 File type and size validation in Form Requests (PDF/JPG/PNG limits per asset type)

## P. Documentation Habit (Phase 1 W1-W7 — saves Day 40 effort)

- [ ] CHK160 Form Requests have `@bodyParam` PHPDoc on every field (Scribe-compatible)
- [ ] CHK161 API Resources have `@response` example with realistic data
- [ ] CHK162 New endpoints reflected in internal Postman/Bruno collection (if maintained)
- [ ] CHK163 ADR updated if architectural decisions emerged during implementation

## Q. Git & Commit Discipline (CLAUDE.md + Constitution governance)

- [ ] CHK170 Commits follow conventional format: `<type>(<module>): <description>`
- [ ] CHK171 Commit per logical unit — not one giant end-of-phase commit
- [ ] CHK172 No `git push` issued by AI agent (deny rule in `.claude/settings.json`)
- [ ] CHK173 Branch naming follows `{phase-id}-{module}-{slug}` pattern (e.g., `1.1-identity-vendor-approval`)

---

## Notes

- Check items off as completed: `[x]`
- Strike out non-applicable items with reasoning: `~~CHK0XX item~~ — N/A because [reason]`
- Add comments inline below an item if context is needed
- Cite constitution principles (I-XI) when adding new items not in this template
- Items numbered with category prefix for easy reference (CHK0XX = Category G, CHK1XX = K-Q)
- Sections A-J apply to every feature; sections K-Q are conditional based on what the feature touches

## Verification Process

1. After `/speckit.specify` — verify A, K (early scope guard)
2. After `/speckit.plan` — verify A, B, C, D, E, F, G, H (architectural decisions)
3. During `/speckit.implement` — verify I (migrations), J (tests), M (Filament if applicable)
4. Before phase exit — verify all applicable categories
5. `/speckit.analyze` runs cross-artifact consistency check that maps to many of these items

## Escalation

If any NON-NEGOTIABLE item (Constitution §II, III, IV, V, VII, VIII, X, XI principles) cannot be satisfied:

1. STOP implementation
2. Document the conflict in an ADR (`ADR-NNNN-conflict-{slug}.md`)
3. Get Ibrahim's explicit approval before proceeding
4. Update the constitution if this is a permanent change

The checklist is a guardrail — failures here are signals to slow down, not bypass.